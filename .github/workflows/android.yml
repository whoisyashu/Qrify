      - name: 📦 Install system dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential \
            ccache \
            libncurses5 \
            libstdc++6 \
            zlib1g \
            zlib1g-dev \
            openjdk-11-jdk \
            unzip \
            git \
            python3-pip \
            libffi-dev \
            libssl-dev \
            libsqlite3-dev \
            libjpeg-dev \
            libfreetype6-dev
